/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'gravatar.com',
            'via.placeholder.com',
        ]
    }
}

module.exports = nextConfig
